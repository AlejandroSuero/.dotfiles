#!/usr/bin/env bash

alias ls=$(exa --icons --classify)
alias ll=$(exa -l --icons --classify)
alias la=$(exa -la --icons --classify)
alias lg=$(exa -la --git --icons --classify)
alias lt=$(exa -la --tree --icons --classify)
