---
- name: "Rust | {{ ansible_distribution }} | Install rustc cargo"
  ansible.builtin.pacman:
    name: "{{ item }}"
    state: present
    update_cache: true
  become: true
  loop:
    - rustc
    - cargo
